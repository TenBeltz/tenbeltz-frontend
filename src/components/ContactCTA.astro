---
import BottomMiddleGradientBackground from '/public/images/img/bottom-middle-gradient-background.png';
import { Picture } from 'astro:assets';
import { ScrollReveal } from './ScrollReveal';
---

<div class="relative">
  <div class="flex flex-col px-12 py-20 text-white gap-y-12">
    <div class="flex flex-col text-center gap-y-6">
      <ScrollReveal client:visible>
        <h3 class="text-3xl font-extrabold">Elige el siguiente paso para tu IA en producto</h3>
      </ScrollReveal>
      <ScrollReveal client:visible delay={100}>
        <h4 class="text-lg font-semibold text-slate-300">Cada fase está pensada para reducir riesgo: diagnóstico, sprint, producción y optimización continua. Descubre cuál encaja mejor con tu momento.</h4>
      </ScrollReveal>
    </div>
    <div class="flex items-center justify-center">
      <ScrollReveal client:visible delay={200}>
        <a href="/#contact-section" class="text-xl font-semibold btn duration-300 bg-pheromone-purple glow-12 hover:glow-16 glow-pheromone-purple hover:glow-pheromone-light">Agenda una consulta</a>
      </ScrollReveal>
    </div>
  </div>
  <Picture src={BottomMiddleGradientBackground} alt="backgroundGradientPC" aria-hidden="true" loading="eager" fetchpriority="high" class="absolute inset-0 object-cover w-full h-full -z-50 lg:block" />
</div>
